#' compressr package
#'
#' @name compressr-package
#' @aliases compressr
#' @docType package
#' @description Simple wrapper functions for tar, untar, zip and unzip from the utils package.
#' 
#' @section pack and unpack:
#' The two primary functions used are \code{\link{pack}} and \code{\link{unpack}}. 
#' The former will package up the specified files or directory as either a 
#' \code{.tar.gz} or \code{.zip} file.  The latter will unpack a compressed file.
NULL
